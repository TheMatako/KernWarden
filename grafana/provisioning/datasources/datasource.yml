apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # The URL relies on Docker's internal DNS resolving the 'prometheus' container name
    url: http://prometheus:9090
    isDefault: true
    editable: false